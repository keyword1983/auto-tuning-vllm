optuna>=3.0.0
ray[default]>=2.0.0
vllm>=0.2.0
pyyaml>=6.0
pydantic>=2.0.0
typer>=0.9.0
rich>=13.0.0
requests>=2.28.0

# Optional PostgreSQL support
# Install with: pip install psycopg2-binary>=2.9.0
# Only required when using PostgreSQL for study storage or logging

# Optional GuideLLM benchmark provider (benchmark_type: "guidellm")
# The default provider is "vllmbench" (vllm's built-in `vllm bench serve`),
# which needs no extra package. Only install guidellm if you need the
# legacy provider - its CLI has changed shape across releases.
# Install with: pip install guidellm>=0.1.0

# Optional dependencies
# For visualization
plotly>=5.0.0
pandas>=2.0.0

# For development
pytest>=7.0.0
black>=23.0.0
isort>=5.0.0
mypy>=1.0.0
